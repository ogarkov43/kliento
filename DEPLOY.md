# 🚀 Инструкция по деплою на GitHub Pages

## Что нужно загрузить на GitHub:

✅ **Загрузить:**
- `src/` - исходный код
- `index.html` - главный HTML файл
- `package.json` и `package-lock.json` - зависимости
- `tsconfig.json`, `tsconfig.node.json` - конфигурация TypeScript
- `vite.config.ts` - конфигурация Vite
- `.gitignore` - исключения для Git
- `.github/workflows/deploy.yml` - автоматический деплой
- `README.md` - документация

❌ **НЕ загружать:**
- `node_modules/` - зависимости (восстанавливаются через npm install)
- `dist/` - собранные файлы (создаются автоматически при деплое)

## Пошаговая инструкция:

### 1. Инициализация Git репозитория
```bash
git init
git add .
git commit -m "Initial commit"
```

### 2. Создайте репозиторий на GitHub
- Перейдите на https://github.com/new
- Создайте новый репозиторий (например, `weny-miniapp`)
- **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### 3. Подключите локальный репозиторий к GitHub
```bash
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/weny-miniapp.git
git push -u origin main
```

### 4. Настройте GitHub Pages
1. Перейдите в Settings → Pages вашего репозитория
2. В разделе "Source" выберите **"GitHub Actions"**
3. Workflow автоматически запустится и соберет проект

### 5. Дождитесь завершения деплоя
- Перейдите во вкладку "Actions" в вашем репозитории
- Дождитесь завершения workflow "Deploy to GitHub Pages"
- Зеленая галочка означает успешный деплой

### 6. Получите ссылку на приложение
После успешного деплоя ваше приложение будет доступно по адресу:
```
https://ВАШ_USERNAME.github.io/weny-miniapp/
```

### 7. Подключите к Telegram Mini App
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Выберите вашего бота или создайте нового (`/newbot`)
3. Используйте команду `/newapp` или `/setmenubutton`
4. Укажите:
   - **Title**: Weny (или любое другое название)
   - **Description**: Планировщик задач и целей
   - **Photo**: (опционально) загрузите иконку
   - **Web App URL**: `https://ВАШ_USERNAME.github.io/weny-miniapp/`

### 8. Проверьте работу
- Откройте вашего бота в Telegram
- Нажмите на кнопку меню или команду для запуска Mini App
- Приложение должно открыться в Telegram

## 🔄 Обновление приложения

После каждого изменения кода:
```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически пересоберет и задеплоит новую версию.

## ⚠️ Важно

- Убедитесь, что в Settings → Pages выбран источник "GitHub Actions"
- Первый деплой может занять несколько минут
- После изменений подождите завершения workflow перед проверкой
