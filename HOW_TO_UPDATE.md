# 🔄 Как обновить приложение в Telegram

## Способ 1: Автоматический (Рекомендуется) ⚡

GitHub Actions автоматически задеплоит изменения при пуше в `main`:

```bash
# 1. Соберите проект
npm run build

# 2. Закоммитьте и запушьте изменения
git add .
git commit -m "Update app"
git push origin main
```

**Готово!** GitHub Actions автоматически:
- Соберет проект
- Загрузит файлы в ветку `gh-pages`
- Обновит сайт через 1-2 минуты

## Способ 2: Ручной (Если Actions не работает) 🔧

### Вариант A: Через скрипт

```bash
./update.sh
```

Скрипт соберет проект и покажет инструкции.

### Вариант B: Вручную

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Загрузите файлы на GitHub:**
   - Откройте: https://github.com/ogarkov43/kliento/tree/gh-pages
   - Нажмите "Add file" → "Upload files"
   - Перетащите **все файлы** из папки `dist`:
     - `index.html`
     - `404.html`
     - Всю папку `assets/` (со всеми файлами)

3. **Нажмите "Commit changes"**

## ⏱️ Время обновления

После загрузки файлов:
- GitHub Pages обновится через **1-2 минуты**
- В Telegram может потребоваться **очистить кэш**:
  - Закройте Mini App
  - Откройте заново
  - Или перезапустите бота

## 🔍 Проверка обновления

Откройте в браузере: https://ogarkov43.github.io/kliento/

Если видите изменения - значит обновление прошло успешно!




