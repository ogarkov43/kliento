# 🔧 Решение проблем с Telegram Mini App

## Проблема: Приложение не открывается в Telegram

### Шаг 1: Проверьте ссылку в BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. Используйте команду `/myapps`
3. Проверьте, что ссылка указана правильно:
   ```
   https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/
   ```
   ⚠️ **Важно:** Ссылка должна начинаться с `https://` и заканчиваться на `/`

### Шаг 2: Проверьте GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings → Pages**
3. Убедитесь, что:
   - Source: **GitHub Actions** (не "Deploy from a branch")
   - Branch: `gh-pages` (создается автоматически)

### Шаг 3: Проверьте деплой

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Найдите workflow "Deploy to GitHub Pages"
3. Убедитесь, что он завершился успешно (зеленая галочка)
4. Если есть ошибки - проверьте логи

### Шаг 4: Проверьте доступность сайта

Откройте ссылку в браузере:
```
https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/
```

**Что должно быть:**
- ✅ Страница открывается без ошибок
- ✅ Нет ошибок в консоли браузера (F12 → Console)
- ✅ Все ресурсы загружаются (CSS, JS файлы)

**Если страница не открывается:**
- Подождите 5-10 минут после деплоя (GitHub Pages может обновляться с задержкой)
- Проверьте, что репозиторий публичный (или у вас есть доступ)

### Шаг 5: Проверьте консоль браузера в Telegram

1. Откройте приложение в Telegram
2. Нажмите на три точки (⋮) в правом верхнем углу
3. Выберите "Open in Browser"
4. Откройте консоль разработчика (F12 или Cmd+Option+I)
5. Проверьте ошибки в консоли

**Частые ошибки:**

#### Ошибка: "Failed to load resource"
- **Причина:** Неправильный base path или файлы не найдены
- **Решение:** Проверьте, что в `vite.config.ts` указан правильный `base: '/'`

#### Ошибка: "CORS policy"
- **Причина:** Проблемы с CORS
- **Решение:** GitHub Pages автоматически поддерживает CORS, но убедитесь, что используете HTTPS

#### Ошибка: "404 Not Found"
- **Причина:** Файлы не задеплоены или неправильный путь
- **Решение:** 
  1. Проверьте, что workflow завершился успешно
  2. Проверьте папку `gh-pages` в репозитории
  3. Убедитесь, что файлы есть в `dist/` после сборки

### Шаг 6: Пересоздайте приложение в BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. Используйте `/deleteapp` для удаления старого приложения
3. Создайте новое через `/newapp`
4. Укажите ссылку заново

### Шаг 7: Проверьте настройки репозитория

Убедитесь, что:
- ✅ Репозиторий публичный (или у вас есть доступ)
- ✅ GitHub Pages включен
- ✅ Workflow имеет права на запись (обычно включено по умолчанию)

### Шаг 8: Проверьте формат ссылки

**Правильный формат:**
```
https://username.github.io/repository-name/
```

**Неправильные форматы:**
```
http://username.github.io/repository-name/  ❌ (не HTTPS)
https://username.github.io/repository-name   ❌ (нет слеша в конце)
https://github.com/username/repository-name  ❌ (это ссылка на репозиторий, не на Pages)
```

## Быстрая диагностика

Выполните эти команды для проверки:

```bash
# 1. Проверьте, что проект собирается локально
npm run build

# 2. Проверьте содержимое dist/
ls -la dist/

# 3. Должны быть файлы:
# - dist/index.html
# - dist/assets/index-*.js
# - dist/assets/index-*.css
```

## Если ничего не помогло

1. **Проверьте логи GitHub Actions:**
   - Перейдите в Actions → выберите последний workflow
   - Проверьте логи каждого шага

2. **Проверьте настройки репозитория:**
   - Settings → Pages → Source должен быть "GitHub Actions"
   - Settings → Actions → General → Workflow permissions должен быть "Read and write permissions"

3. **Попробуйте другой способ деплоя:**
   - Используйте Vercel или Netlify как альтернативу GitHub Pages

4. **Создайте issue на GitHub:**
   - Опишите проблему
   - Приложите скриншоты ошибок
   - Укажите ссылку на репозиторий
