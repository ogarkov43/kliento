#!/bin/bash

# Скрипт для быстрого деплоя папки dist

echo "🔨 Сборка проекта..."
npm run build

if [ $? -ne 0 ]; then
    echo "❌ Ошибка при сборке проекта"
    exit 1
fi

echo "✅ Проект собран успешно!"
echo ""
echo "Выберите способ деплоя:"
echo "1) Vercel CLI"
echo "2) Только сборка (без деплоя)"
echo ""
read -p "Ваш выбор (1-2): " choice

case $choice in
    1)
        echo "🚀 Деплой в Vercel..."
        cd dist
        vercel --prod
        ;;
    2)
        echo "✅ Проект собран в папку dist"
        echo "Теперь вы можете задеплоить папку dist вручную"
        ;;
    *)
        echo "❌ Неверный выбор"
        exit 1
        ;;
esac

